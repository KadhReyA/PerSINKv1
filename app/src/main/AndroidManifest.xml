<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.persink">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".pengaturan_notifikasi">
        </activity>
        <activity
                android:name=".stnk"
                android:label="@string/title_activity_stnk"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".sim"
                android:label="sim"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".splash.splash"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <receiver
                android:name=".AlarmReceiver"
                android:enabled="true"/>

        <service
                android:name=".NotificationService"
                android:enabled="true"/>

        <activity
                android:name=".lihatsim"
                android:label="lihatsim"
                android:theme="@style/AppTheme.NoActionBar"/>
        <activity
                android:name=".lihatstnk"
                android:label="lihatstnk"
                android:theme="@style/AppTheme.NoActionBar"/>
        <activity
                android:name=".panduan"
                android:label="Panduan">
        </activity>
    </application>

</manifest>